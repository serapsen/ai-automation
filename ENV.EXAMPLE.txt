# Copy to .env and fill values
# General
LOG_LEVEL=INFO


# Azure OpenAI (optional; preferred when configured)
# DALL-E 3 requires openai>=1.0.0. Set your Azure endpoint, key, and deployment name.
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=dall-e-3
# API version for Images
OPENAI_API_VERSION=2024-04-01-preview
# Optional rendering hints
AZURE_OPENAI_IMAGE_STYLE=vivid
AZURE_OPENAI_IMAGE_QUALITY=standard

# Azure Blob Storage (choose one auth method)
# Preferred: connection string
AZURE_STORAGE_CONNECTION_STRING=
# Or account + key
AZURE_STORAGE_ACCOUNT=
AZURE_STORAGE_KEY=
# Target container and optional prefix
AZURE_BLOB_CONTAINER=ai-automation
AZURE_BLOB_PREFIX=images

# Storage backend selection (azure|none). Defaults to 'azure' if unset.
# Set to 'none' to disable cloud uploads for local demos.
STORAGE_BACKEND=none

# SMTP (Email)
# Required for email notifications. Choose appropriate TLS mode based on provider.
# General keys
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
# Optional global fallback recipients; briefs can override via `notifications`.
SMTP_TO=
SMTP_CC=
# TLS mode: set either implicit SSL (465) or STARTTLS (587)
SMTP_SSL=false
SMTP_STARTTLS=true
# Attach run summaries (JSON/CSV) to emails
EMAIL_ATTACH_SUMMARY=true
